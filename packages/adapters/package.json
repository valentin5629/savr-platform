{
  "name": "@savr/adapters",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./src/index.js": "./src/index.ts",
    "./src/outbox-worker.js": "./src/outbox-worker.ts"
  },
  "scripts": {
    "build": "echo 'adapters: build no-op (paquet TS-only consommé via main)'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.1",
    "@savr/shared": "workspace:*"
  }
}
