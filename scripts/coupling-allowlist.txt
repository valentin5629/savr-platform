# =============================================================================
# Allowlist du garde-fou 3 (anti-couplage TMS) — scripts/check-coupling.sh
# =============================================================================
# Un chemin liste ici est EXEMPTE du motif "mts1" / "everest" uniquement.
# Le motif "customerOrders" n'est JAMAIS exempte, meme via ce fichier.
#
# Regles d'usage :
#   - 1 glob de chemin par ligne (relatif a la racine du repo). `*` traverse `/`.
#   - Toute ligne ajoutee ici est VISIBLE EN PR → l'erosion du garde-fou est
#     revue, jamais silencieuse. Justifier chaque entree en commentaire.
#   - N'exempter que des CONSTANTES legitimes (valeurs d'enum / seed), jamais
#     de la LOGIQUE metier qui appelle un transporteur (celle-ci va en adapters).
#
# Cas anticipe : la valeur d'enum transporteurs.type_tms = 'mts1' | 'everest'
# vit dans la migration qui cree le type + le seed du referentiel transporteurs.
# Decommenter et ajuster le nom exact quand la migration sera ecrite :
#
#   supabase/migrations/*_plateforme_transporteurs*.sql
#   supabase/seed.sql
#
# Exceptions actives (module 0.3 — schema DB) :
#   - type_tms ('mts1','a_toutes','autre') : valeur d'enum §04 Data Model, pas logique metier
#   - code_transporteur_mts1 / id_point_collecte_mts1 : colonnes V1-only liste fermee garde-fou 1
#   - last_everest_ping_at/status : colonne operationnelle prestataires (V1.1, pas logique adapter)
supabase/migrations/20260611171637_plateforme_bloc3_parametres_associations_transporteurs.sql
supabase/migrations/20260611171635_shared_plateforme_bloc1_orgs_users.sql
# specs/ = miroir dérivé du Vault CDC (lecture seule, jamais de logique métier)
# Les occurrences mts1/everest = valeurs d'enum dans le DDL cible V2 + scénarios QA
specs/*
# supabase/tests/M0_6__* = fixtures pgTAP — 'mts1' = valeur de l'enum type_tms
# dans un INSERT de transporteur de test (pas de logique adapter)
supabase/tests/M0_6__cat_4.test.sql
# packages/shared/src/seed/* = scripts de seed (M0.7) — 'mts1' = valeur d'enum
# transporteurs.type_tms / shared.prestataires.mode_integration + libellés de
# logs d'intégration (données fictives, jamais d'appel réel). Aucune logique
# adapter ici (elle vit dans packages/adapters/). 'customerOrders' reste banni.
packages/shared/src/seed/*
# supabase/migrations/20260613000001_* = vue ops v_ops_integrations — 'mts1' = label
# de ligne SQL (données statiques de la vue, liste des services surveillés).
# Pas de logique adapter : la vue est lue par la page Santé système (Admin), jamais
# par un adapter. 'customerOrders' reste banni.
supabase/migrations/20260613000001_plateforme_health_views_ops.sql
# packages/plateforme/.../transporteurs/* (M1.1b) — 'mts1' = valeur de l'enum type_tms
# dans le CRUD référentiel transporteurs (UI + API route). Validation métier :
# code_transporteur_mts1 obligatoire si type_tms='mts1' (§04 Data Model, colonne V1-only
# liste fermée garde-fou 1). Aucun appel HTTP MTS-1 ici — la logique adapter est dans
# packages/adapters/. 'customerOrders' reste banni.
packages/plateforme/src/app/(admin)/admin/transporteurs/*
packages/plateforme/src/app/api/v1/admin/transporteurs/*
packages/plateforme/tests/api/admin/transporteurs.m1-1b.test.ts
# packages/plateforme/.../webhooks/everest/* (M2.5) — endpoint récepteur du webhook Everest.
# "everest" = nom du transporteur dans les logs/champs `integration` de la table integrations_logs
# et valeur métier de `source='everest'` dans integrations_inbox. Aucun appel HTTP Everest ici :
# les appels sortants passent par packages/adapters/everest/. 'customerOrders' reste banni.
packages/plateforme/src/app/api/webhooks/everest/route.ts
packages/plateforme/tests/api/webhooks/everest.m2-5.test.ts
# packages/plateforme/.../admin/everest/* (M2.5) — endpoint Admin de secours pour acceptation
# manuelle (Everest down, Ops a appelé A Toutes! par téléphone). "everest" = nom de la table
# `everest_missions` (schéma plateforme) + message d'erreur UI. Aucun appel HTTP. 'customerOrders' banni.
packages/plateforme/src/app/api/v1/admin/everest/missions/manual-accept/route.ts
# supabase/migrations/2026061521/22/23* = migrations M2.5 — 'everest' = nom de la table
# `everest_missions` + de l'enum `statut_mission_everest` + colonnes + policies RLS.
# Données de schéma uniquement, jamais de logique adapter. 'customerOrders' reste banni.
supabase/migrations/20260615210000_plateforme_m2_5_everest.sql
supabase/migrations/20260615220000_plateforme_m2_5_everest_fix.sql
supabase/migrations/20260615230000_plateforme_m2_5_everest_rls_v2.sql
# supabase/migrations/20260629110000_* (R10a/BL-P1-API-04) — élargit le CHECK
# chk_everest_service_id_values de la table `everest_missions` (ajout service 77).
# 'everest' = nom de table + colonne `everest_service_id`. Schéma DB uniquement,
# aucune logique adapter ni appel HTTP. 'customerOrders' reste banni.
supabase/migrations/20260629110000_plateforme_r10a_everest_service_77.sql
# supabase/tests/r10a_everest_service_77.test.sql (R10a) — pgTAP guard du CHECK
# ci-dessus. 'everest' = nom de table `everest_missions` dans les INSERT fixtures.
# Aucune logique adapter. 'customerOrders' reste banni.
supabase/tests/r10a_everest_service_77.test.sql
# supabase/migrations/*_m2_3_algo* (M2.3) — 'mts1' = valeur de type_tms dans SELECT
# de transporteur par type (recherche Marathon par type_tms='mts1' dans la DB). 'everest' =
# dans les commentaires de code décrivant les services (71/74/77). Aucun appel HTTP MTS-1/Everest
# ici — toute logique adapter vit dans packages/adapters/. 'customerOrders' reste banni.
supabase/migrations/20260615240000_plateforme_m2_3_algo_attribution_ag.sql
# supabase/migrations/20260625000000_* (R5/BL-P0-08) — 'mts1'/'a_toutes' = valeurs de
# l'enum type_tms dans le CASE qui DÉRIVE le consumer outbox (mts1→adapter_mts1,
# a_toutes→adapter_everest, autre→provider_manual) ; 'Everest' en commentaire. AUCUN
# appel HTTP : la fonction émet seulement un event outbox, l'adapter (packages/adapters/)
# le consomme. 'customerOrders' reste banni.
supabase/migrations/20260625000000_plateforme_r5_dispatch_ag_bridge.sql
# supabase/migrations/20260702020200_* (R17b/BL-P1-BOA-02, Val 2026-07-02) — CREATE OR
# REPLACE VERBATIM de rpc_valider_attribution_ag (copie du R5 ci-dessus) : seule la ligne
# du CASE change (ELSE→provider_manual pour router 'autre'/'par_mail'/'par_telephone' →
# dispatch manuel). Mêmes 'mts1'/'adapter_mts1' (valeur enum type_tms → consumer). Aucun
# appel HTTP — la fonction émet un event outbox, l'adapter le consomme. 'customerOrders' banni.
supabase/migrations/20260702020200_plateforme_r17b_dispatch_par_mail_telephone.sql
# supabase/migrations/20260620100000_* (fix E2 timezone) — CREATE OR REPLACE de
# fn_calculer_algo_attribution_ag : corps copié VERBATIM de la source M2.3 ci-dessus
# (seule la ligne de calcul du délai change), donc mêmes 'mts1' (enum type_tms) +
# commentaires services Everest 71/74/77/91. Aucune logique adapter. 'customerOrders' reste banni.
supabase/migrations/20260620100000_plateforme_fix_tz_seuil_12h_ag.sql
# supabase/tests/m2_3_algo* (M2.3) — 'mts1' = valeur enum dans INSERT fixtures pgTAP
# (transporteurs.type_tms + prestataires.mode_integration). Pas de logique adapter.
supabase/tests/m2_3_algo_attribution_ag.test.sql
# packages/plateforme/.../parametres/algo-ag/* (M2.3) — 'Everest' = libellé UI de la clé
# paramétre algo 'everest_codes_postaux' (données statiques, description de la table parametres_algo).
# Aucun appel HTTP. 'customerOrders' reste banni.
packages/plateforme/src/app/(admin)/admin/parametres/algo-ag/page.tsx
# packages/plateforme/tests/.../algo-attribution-ag* (M2.3) — 'mts1' = valeur enum dans
# objet mock fixture transporteur (type_tms:'mts1'). Pas de logique adapter.
packages/plateforme/tests/api/admin/algo-attribution-ag.m2-3.test.ts
# packages/plateforme/tests/api/cron/cron-auth.test.ts (fix audit BUG 3) — 'mts1' = segment
# du chemin de la route cron testée (@/app/api/cron/polling-mts1/route.js) + libellés de test.
# Le test vérifie UNIQUEMENT la garde d'auth fail-closed (401) de l'endpoint cron ; il importe
# la route via son chemin et mocke l'adapter. Aucune logique adapter ni appel HTTP MTS-1 ici
# (elle vit dans packages/adapters/). 'customerOrders' reste banni.
packages/plateforme/tests/api/cron/cron-auth.test.ts
# docs/audit/*.mjs = scripts de recompte/reconstruction des registres d'audit conformité
# CDC→code (build_rerun_*.mjs, reconcile_*.mjs). 'everest'/'mts1' y apparaissent uniquement
# en PROSE des registres générés (ex. « warning Everest #savr-alerts-info »), jamais comme
# logique métier ni appel HTTP — ce sont des outils de documentation hors runtime applicatif.
# 'customerOrders' reste banni (jamais présent dans ces scripts).
docs/audit/*.mjs
# packages/shared/src/database.types.ts (G7, lot R0a) — types DB GENERES par
# `supabase gen types` (jamais edites a la main). 'mts1'/'everest' y apparaissent
# uniquement comme NOMS DE SCHEMA reels et legitimes : table `everest_missions`,
# colonnes V1-only `code_transporteur_mts1` / `id_point_collecte_mts1`, valeur
# d'enum `transporteurs.type_tms = 'mts1'`. Aucune logique adapter ni appel HTTP
# (un fichier de types ne contient que des declarations). 'customerOrders' reste
# banni (verifie : 0 occurrence).
packages/shared/src/database.types.ts
# supabase/migrations/20260630120000_* (R11) — CREATE OR REPLACE de
# fn_calculer_algo_attribution_ag (corps copie de la source M2.3 ci-dessus + province
# top 3) : 'mts1' = valeur enum type_tms dans le SELECT du transporteur, 'Everest' en
# commentaires services 71/74/77/91 + rpc_evaluer_auto_accept_ag /
# rpc_log_attribution_aucune_reco. Aucune logique adapter ni appel HTTP — le dispatch
# passe par l'outbox (R5). 'customerOrders' reste banni.
supabase/migrations/20260630120000_plateforme_r11_algo_ag.sql
# supabase/tests/r11_algo_ag.test.sql (R11) — 'mts1' = valeur enum type_tms dans les
# INSERT fixtures pgTAP (transporteurs province + Marathon). Pas de logique adapter.
# 'customerOrders' reste banni.
supabase/tests/r11_algo_ag.test.sql
# packages/plateforme/.../components/admin/collecte-detail-panel.tsx (+ test)
# (R17/BL-P1-BOA-06, déplacé E2E « liste type maquette + pop-up ») — fiche collecte
# Bloc 0 dispatch, ex-page collectes/[id]/page.tsx, extraite en composant monté
# dans une modale centrée sur la liste. 'mts1' = valeur de l'enum transporteurs.type_tms
# utilisée UNIQUEMENT pour forker le LIBELLÉ du bouton d'envoi (helper libelleDispatch :
# type_tms='mts1' → « Envoyer à MTS-1 »). Aucun appel HTTP MTS-1/Everest : le dispatch passe
# par l'endpoint /dispatch → outbox → adapter (packages/adapters/). Même nature d'exception
# que transporteur-form.tsx (UI keyée sur l'enum type_tms). 'customerOrders' reste banni.
# NB : la route collectes/[id]/page.tsx n'est plus qu'une redirection vers ?collecte=<id>
# (0 réf mts1) → retirée de l'allowlist ; le wrapper collecte-detail-modal.tsx n'a aucune réf.
packages/plateforme/src/components/admin/collecte-detail-panel.tsx
packages/plateforme/src/components/admin/collecte-detail-panel.m0-6.test.tsx
# collectes.m1-1b.test.ts (BL-P1-BOA-06) — 'mts1' = valeur de l'enum type_tms dans
# une fixture d'objet algoReco (transporteur mocké du test dispatch AG motif-vs-top1).
# Aucun appel HTTP. 'customerOrders' reste banni.
packages/plateforme/tests/api/admin/collectes.m1-1b.test.ts
# packages/plateforme/.../admin/transporteur-modal.* (E2E — modale liste transporteurs) —
# même nature d'exception que transporteur-form.tsx : UI keyée sur l'enum transporteurs.type_tms
# (affichage conditionnel du champ code_transporteur_mts1 + validation « obligatoire si
# type_tms='mts1' », §04 Data Model). Aucun appel HTTP MTS-1 : la logique adapter vit dans
# packages/adapters/. 'customerOrders' reste banni.
packages/plateforme/src/components/admin/transporteur-modal.tsx
packages/plateforme/src/components/admin/transporteur-modal.test.tsx
# Cliquet des gardes API (sécurité, 2026-09-11) : nomme la route du webhook logistique pour
# vérifier sa garde propre (jeton X-Webhook-Token). Aucun appel HTTP ; 'customerOrders' reste banni.
packages/plateforme/tests/securite/api-routes-gardees.test.ts
<<<<<<< HEAD
# supabase/migrations/20260915170000_* + son pgTAP — index UNIQUE PARTIEL sur
# transporteurs.prestataire_logistique_id (1 prestataire = au plus 1 transporteur).
# 'mts1'/'a_toutes'/'Everest' y sont des VALEURS DE L'ENUM type_tms (INSERT de fixtures,
# et prose expliquant POURQUOI le 1:1 est ce qui rend le cloisonnement par provider
# possible). C'est le cas anticipé en tête de ce fichier : « la valeur d'enum
# transporteurs.type_tms vit dans la migration qui crée le type + le seed ».
# Aucune logique adapter, aucun appel HTTP — une contrainte d'intégrité et ses
# assertions. 'customerOrders' reste banni (vérifié : 0 occurrence dans les deux).
supabase/migrations/20260915170000_plateforme_transporteurs_presta_unique.sql
supabase/tests/SECU__transporteur_presta_unique.test.sql
# supabase/migrations/20260915180000_* + son pgTAP (invariants du cloisonnement
=======
# supabase/migrations/20260915210000_* + son pgTAP (invariants du cloisonnement
>>>>>>> 3e0bad2 (chore(db): renumérote les deux migrations — 20260915180000 est déjà pris sur dev)
# provider) — 'mts1'/'a_toutes'/'everest' n'apparaissent que (a) dans les commentaires
# qui expliquent POURQUOI l'index existe et (b) comme valeurs de l'enum
# transporteurs.type_tms dans les fixtures pgTAP. La migration ne pose que deux index
# UNIQUE partiels : aucune logique adapter, aucun appel HTTP, aucun nom de ressource
# distante. Même nature d'exception que 20260625000000 (bridge dispatch AG).
# 'customerOrders' reste banni.
supabase/migrations/20260915210000_plateforme_cloisonnement_provider_invariants.sql
supabase/tests/SECU__cloisonnement_provider_invariants.test.sql
# Fixtures pgTAP du gate E2 (2026-09-15) — 'mts1'/'a_toutes' = valeurs de l'enum
# transporteurs.type_tms dans des INSERT de fixtures. Depuis que le gate compare le
# type_tms du prestataire de la collecte a celui du prestataire de la tournee, ces
# fixtures doivent poser le transporteur (sinon la collecte n'est « commandee » chez
# aucun provider et les cas E2 passeraient au vert pour la mauvaise raison). Aucune
# logique adapter, aucun appel HTTP. 'customerOrders' reste banni.
supabase/tests/e2_gate_commande_provider.test.sql
supabase/tests/edition_evenement.test.sql
supabase/tests/r22c_evenement_e2_en_cours.test.sql
# supabase/tests/SECU__evenements_ecriture_client_fermee.test.sql — fixture : le
# transporteur qui porte le prestataire de la collecte commandée (type_tms = valeur
# d'enum). Requis depuis #327 : le gate E2 compare le type_tms des deux
# transporteurs. Aucune logique d'adapter.
supabase/tests/SECU__evenements_ecriture_client_fermee.test.sql
# supabase/migrations/20260915220000_* (gate E2 provider-aware) — 'everest' apparait
# dans le commentaire qui explique pourquoi le gate seul peut rattraper un mauvais
# type d'event chez ce provider ; 'mts1' est la valeur de l'enum type_tms dans le
# transporteur du harnais pgTAP `tests.outbox_fixture_collecte`. Aucune logique
# adapter, aucun appel HTTP. 'customerOrders' reste banni.
supabase/migrations/20260915220000_plateforme_gate_e2_dimension_provider.sql
# Test de la route d'acceptation manuelle Everest (2026-09-15) — même nature
# d'exception que la route qu'il couvre (déjà allowlistée) : 'everest' est le nom
# de la table `everest_missions` et du segment d'URL, 'mts1'/'a_toutes' sont des
# valeurs de l'enum transporteurs.type_tms dans les fixtures. Le test prouve que
# le rattachement de la mission vise la tournée du BON provider. Aucun appel HTTP.
# 'customerOrders' reste banni.
packages/plateforme/tests/api/admin/everest-manual-accept.m2-5.test.ts

# packages/plateforme/src/lib/transporteur-lien-prestataire.ts (lien transporteur →
# prestataire logistique) — CONSTANTE d'enum `transporteurs.type_tms` : la liste
# des types routés vers un adapter, pour lesquels le lien est obligatoire (sans
# lui `prestatairesDuType` lève et tous les événements partent en DLQ). Partagée
# par les routes admin transporteurs ET la modale (déjà exemptées) pour n'avoir
# qu'une définition. Aucun appel à un transporteur, aucune logique d'adapter.
packages/plateforme/src/lib/transporteur-lien-prestataire.ts

# 20260916150000 transporteurs_cols_immuables (arbitrage Val 2026-09-16) — la
# migration ne cite « mts1 » que dans son commentaire de mesure des données ; le
# pgTAP porte les VALEURS d'enum transporteurs.type_tms / prestataires.mode_integration
# dans ses fixtures et assertions. Deux triggers d'intégrité (immuabilité, garde
# DELETE) : aucune logique adapter, aucun appel HTTP. 'customerOrders' reste banni.
supabase/migrations/20260916150000_plateforme_transporteurs_cols_immuables.sql
supabase/tests/SECU__transporteur_cols_immuables.test.sql
# Acceptation manuelle d'une mission A Toutes! avec référence obligatoire (2026-09-16,
# §06.06 §3 Bloc 0) — RPC `fn_accepter_mission_everest_manuelle` et son pgTAP : 'everest'
# = nom de la table `everest_missions` / de la colonne `everest_mission_id` et du segment
# d'URL de la route déjà allowlistée ; 'mts1' = valeur de l'enum type_tms dans les INSERT
# de fixtures. Écritures DB uniquement, aucun appel HTTP transporteur. 'customerOrders' banni.
supabase/migrations/20260916160000_plateforme_everest_acceptation_manuelle_reference.sql
supabase/tests/everest_acceptation_manuelle_reference.test.sql
# CHECK `chk_everest_created_manually` passé en implication (2026-09-16, divergence M2.5)
# + son pgTAP de cycle de vie : 'everest' = nom de la table `everest_missions`, de la
# contrainte et de la colonne `everest_mission_id`, et mot « webhook Everest » dans les
# commentaires. Une contrainte CHECK et des UPDATE de statut en SQL : aucune logique
# adapter, aucun appel HTTP transporteur. 'customerOrders' banni.
supabase/migrations/20260916170000_plateforme_everest_created_manually_implication.sql
supabase/tests/everest_created_manually_cycle_de_vie.test.sql
# Acceptation manuelle A Toutes! : refus depuis rejetee_par_prestataire (2026-09-16,
# plan acceptation_manuelle_everest_statut_depart) + pgTAP des plans du scénario :
# 'everest' = nom de la RPC `fn_accepter_mission_everest_manuelle`, de la table
# `everest_missions` et de la colonne `everest_mission_id`. Écritures DB uniquement,
# aucun appel HTTP transporteur. 'customerOrders' banni.
supabase/migrations/20260916180000_plateforme_everest_acceptation_manuelle_refus_rejetee.sql
supabase/tests/everest_acceptation_manuelle_plans.test.sql
