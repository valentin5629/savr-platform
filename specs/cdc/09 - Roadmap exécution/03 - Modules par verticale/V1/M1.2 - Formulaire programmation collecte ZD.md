# Brief Claude Code — Module 1.2 — Formulaire programmation collecte ZD

> Révisé 2026-06-11 (conventions `12`). ⚠ Ordre réel : M1.2 s'exécute **après M1.3** (le formulaire consomme la fonction tarif).

## Objectif
Le formulaire 3 étapes événement-centré, volet ZD : un traiteur commercial programme une collecte ZD (événement → lieu/contacts → récap), avec autocomplétion, calcul tarif ZD automatique, email de confirmation.

## Contexte (lu depuis CLAUDE.md)
§4 (règles SI/ALORS collecte), §10 (glossaire collecte/événement/lieu). Tarif ZD calculé via M1.3 (dépendance).

## Périmètre — À coder
- [ ] Formulaire 3 étapes : événement (nom client, logo, date événement, pax, type ☐ZD) → lieu/contacts → récap
- [ ] Autocomplétion lieux (référentiel) + saisie manuelle hors référentiel
- [ ] Autocomplétion contacts traiteurs
- [ ] Rattachement explicite `evenement_id` ; `date_evenement` distincte de `date_collecte` (`date_evenement` nullable, décision F1 06.01)
- [ ] Endpoint `POST /api/collectes` (création ZD `statut=programmee`)
- [ ] Email confirmation programmation (transverse C)

## Périmètre — Hors scope (ne PAS toucher)
- [ ] Volet AG (pack, blocage) → V2 M2.2
- [ ] Calcul tarif → M1.3 (appeler la fonction, ne pas la réimplémenter)
- [ ] Émission outbox → M1.4 (le formulaire crée la collecte ; l'outbox est branchée en M1.4)
- [ ] Multi-camions : décision **Ops** (`nb_camions_demande`, back-office) — rien dans le formulaire client

## Pièges connus / décisions à ne PAS rouvrir
- `date_evenement` nullable (F1) ; pax=0 supprimé (F4).
- **Pas de délai minimum bloquant** de programmation (tranché 2026-06-10) : warning < 48h seul.
- Email = vouvoiement, FR, 0 emoji, signature « L'équipe Savr » (templates seed DB, pas d'UI d'édition V1).

## Sources CDC à lire (uniquement — `specs/cdc/` dans le repo)
- `specs/cdc/01 - Cahier des charges App/06 - Fonctionnalités détaillées/01 - Formulaire de programmation de collecte.md`
- `specs/cdc/01 - Cahier des charges App/05 - Règles métier.md` (validation collecte ZD)

## Tests à passer
- Scénarios : `specs/tests/06.01-formulaire-programmation-scenarios.md` (60 scénarios / 76 cas). **Manifest `specs/manifests/M1.2.json` = volet ZD uniquement** (les scénarios AG/pack restent à M2.2).

## Mocks consommés
Mock Resend (0.11) pour l'assertion d'envoi email.

## Définition de fini
- [ ] Manifest M1.2 vert ; CI verte (lint + types + Vitest + Playwright formulaire + build)
- [ ] Démo : un `traiteur_commercial` programme une collecte ZD valide de bout en bout, reçoit l'email de confirmation, la collecte apparaît `programmee`

## Pré-requis d'entrée
- [ ] M1.1 mergé (back-office, référentiel lieux/contacts) ; **M1.3 mergé** (tarif ZD)
- [ ] `specs/` synchronisé ; manifest M1.2 présent
- [ ] `pnpm seed:demo` passe

## Condition d'achèvement /goal
> /goal Le module 1.2 est terminé quand `pnpm test:module M1.2` est vert (échoue si 0 test) ET `pnpm check:coverage M1.2` est vert ET `pnpm build` passe ET `pnpm lint` ne renvoie aucune erreur ET le parcours Playwright "programmer une collecte ZD" est vert.

## Budget tokens estimé
~700k (L).

## Garde-fous /goal
Trust mode ; un seul /goal ; checkpoint humain avant M1.4 ; stop si +50% ; `/cost` → tracker.

## Si tu hésites
Demande à Val. Stop si zone d'ombre hors CLAUDE.md §4/§7.
