# Brief Claude Code — Module 1.3 — Tarification ZD

> Révisé 2026-06-11 (conventions `12`). ⚠ Ordre réel : M1.3 s'exécute **avant M1.2** (le formulaire consomme cette fonction).

## Objectif
Fonction de calcul du tarif ZD (base + remises gestionnaire), **versionnée** (jamais modifiée rétroactivement), avec tests exhaustifs sur les paliers et cas limites.

## Contexte (lu depuis CLAUDE.md)
§4 : « Tarifs ZD versionnés, jamais modifiés rétroactivement ». Tarifs préférentiels gestionnaire appliqués automatiquement.

## Périmètre — À coder
- [ ] Fonction `calculer_tarif_zd(pax, lieu/gestionnaire, date)` lisant la grille **versionnée** active à la date
- [ ] Application remises préférentielles gestionnaire
- [ ] Tests Vitest exhaustifs : tous les paliers + cas limites (bornes de tranches)
- [ ] Migration grille tarifaire versionnée (table + seed grille réelle)

## Périmètre — Hors scope (ne PAS toucher)
- [ ] Tarif AG / packs → V2
- [ ] UI formulaire → M1.2 (consomme cette fonction)
- [ ] Facturation → M1.7

## Pièges connus / décisions à ne PAS rouvrir
- Versionnage = nouvelle version de grille, **jamais** d'UPDATE rétroactif d'une grille existante.
- La grille seedée = grille réelle (`specs/fixtures/`), pas une grille inventée.

## Sources CDC à lire (uniquement — `specs/cdc/` dans le repo)
- `specs/cdc/01 - Cahier des charges App/05 - Règles métier.md` (section Tarification ZD)
- `specs/fixtures/` (grille réelle ZD)

## Tests à passer
- Scénarios : `specs/tests/06.01-formulaire-programmation-scenarios.md` (volet tarif). **Manifest `specs/manifests/M1.3.json`** = scénarios tarif + bornes de paliers.

## Mocks consommés
Aucun.

## Définition de fini
- [ ] Manifest M1.3 vert (tous paliers + cas limites) ; CI verte (lint + types + Vitest + build)
- [ ] Démo : pour un pax donné et un gestionnaire donné, le tarif retourné correspond à la grille versionnée active

## Pré-requis d'entrée
- [ ] M1.1 mergé (référentiel gestionnaire/lieux) ; grille seedée
- [ ] `specs/` synchronisé ; manifest M1.3 présent

## Condition d'achèvement /goal
> /goal Le module 1.3 est terminé quand `pnpm test:module M1.3` est vert (échoue si 0 test) ET `pnpm check:coverage M1.3` est vert ET `pnpm build` passe ET `pnpm lint` ne renvoie aucune erreur.

## Budget tokens estimé
~200k (S/M) — module logique pur, peu d'UI.

## Garde-fous /goal
Trust mode ; un seul /goal ; checkpoint humain avant M1.2 ; stop si +50% ; `/cost` → tracker.

## Si tu hésites
Demande à Val. La grille tarifaire versionnée ne se modifie jamais rétroactivement — en cas de doute sur une valeur, stop.
