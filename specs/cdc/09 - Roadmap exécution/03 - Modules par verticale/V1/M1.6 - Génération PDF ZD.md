# Brief Claude Code — Module 1.6 — Génération PDF ZD (bordereau + rapport recyclage)

> **Régénéré 2026-06-11** — ajoute les règles batch issues de la revue adversariale/idempotence MTS-1 : skip pesées incomplètes (jamais de bordereau vide) + escalade > 48h (R9).

## Objectif
Le moteur PDF (transverse A) + la file async (transverse B) : génération du bordereau de pesée ZD et du rapport de recyclage ZD, batch cron J+1 6h, embargo H+24 sur le rapport, stockage R2.

## Contexte (lu depuis CLAUDE.md)
§2 (PDF Railway/Puppeteer, file `jobs_pdf`, retry 15min/4h ; R2 ; embargo H+24 ; **idempotence entrante : skip batch pesées incomplètes**), §4 (escalade R9), §12 (cron J+1 6h).

## Périmètre — À coder
- [ ] Moteur PDF Railway/Puppeteer (transverse A) + file `jobs_pdf` + retry 15 min/4h (transverse B)
- [ ] Template bordereau pesée ZD
- [ ] Template rapport recyclage ZD (logo client organisateur, embargo H+24)
- [ ] Batch cron J+1 6h (génération bordereaux + rapports des collectes `realisee`)
- [ ] **Skip pesées incomplètes** : le batch J+1 **skippe** les collectes aux pesées incomplètes — jamais de bordereau vide
- [ ] **Escalade R9** : collecte skippée > 48h (pesées toujours incomplètes) = escalade **saisie manuelle Admin** (alerte Ops in-app + flux de saisie)
- [ ] Stockage R2 + référencement `shared.fichiers` (transverse E) ; accès depuis back-office Admin

## Périmètre — Hors scope (ne PAS toucher)
- [ ] Attestation don AG 2041-GE → V2 M2.4 (réutilisera A)
- [ ] Régénération manuelle côté traiteur → V3 (M3.1)
- [ ] Export PDF registre formaté → V1.1
- [ ] Complétion des pesées elle-même (M1.5) — M1.6 lit l'état, ne le corrige pas

## Pièges connus / décisions à ne PAS rouvrir
- Embargo H+24 calé sur `realisee_at`, qui n'est **jamais écrasé** (agrégation no-op M1.5c) — ne pas recalculer l'embargo sur un autre timestamp.
- Rapport « sans excédent » = ligne `rapports_rse` standard **sans embargo** (F1 11-12).
- Jamais de bordereau vide, même « provisoire ».

## Sources CDC à lire (uniquement — `specs/cdc/` dans le repo)
- `specs/cdc/01 - Cahier des charges App/12 - Reporting et exports.md` (rapport recyclage, embargo)
- `specs/cdc/01 - Cahier des charges App/05 - Règles métier.md` (déclenchement à `realisee`, embargo H+24, skip/escalade)
- `specs/cdc/01 - Cahier des charges App/06 - Fonctionnalités détaillées/03 - Registre réglementaire (UX).md` (bordereau)

## Tests à passer
- Scénarios : `specs/tests/11-12-dashboards-reporting-scenarios.md` (génération, embargo). **Manifest `specs/manifests/M1.6.json`** — inclure : skip pesées incomplètes, escalade 48h, retry file, embargo.

## Mocks consommés
Mock Railway/Puppeteer non requis (service interne — tests d'intégration sur l'instance dev) ; horloge contrôlable (fake timers) pour batch J+1 / embargo / 48h.

## Définition de fini
- [ ] Manifest M1.6 vert ; CI verte (lint + types + Vitest + build)
- [ ] Démo : une collecte ZD `realisee` complète → batch J+1 génère bordereau + rapport (embargo H+24), stockés R2, accessibles back-office ; une collecte aux pesées incomplètes → skippée sans bordereau, escaladée à 48h

## Pré-requis d'entrée
- [ ] M1.5 mergé (pesées synchronisées, `pesees_tournees`) ; transverses A/B/E posés ; Railway + R2 configurés (0.2)
- [ ] `specs/` synchronisé ; manifest M1.6 présent

## Condition d'achèvement /goal
> /goal Le module 1.6 est terminé quand `pnpm test:module M1.6` est vert (échoue si 0 test ; inclut génération + embargo H+24 + skip pesées incomplètes + escalade 48h + retry file) ET `pnpm check:coverage M1.6` est vert ET `pnpm build` passe ET `pnpm lint` ne renvoie aucune erreur ET un test e2e confirme le bordereau et le rapport produits et stockés sur R2.

## Budget tokens estimé
~900k (L) — inclut la pose des transverses A + B.

## Garde-fous /goal
Trust mode ; un seul /goal ; checkpoint humain avant M1.7 ; stop si +50% ; `/cost` → tracker.

## Si tu hésites
Demande à Val. SLA PDF async = 5 s e2e (CLAUDE.md §16).
