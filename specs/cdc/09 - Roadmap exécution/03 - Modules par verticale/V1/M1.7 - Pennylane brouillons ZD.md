# Brief Claude Code — Module 1.7 — Pennylane brouillons ZD + AG

> Révisé 2026-06-11 (conventions `12` : mock Pennylane en CI, sandbox = vérif manuelle).

## Objectif
Création des brouillons de facture ZD (mode par collecte + mode mensuel groupé par traiteur) **et AG (unitaire uniquement : par pack ou par collecte)**, push vers Pennylane API v2 après validation Admin, numérotation séquentielle gapless, polling J+1 (webhook = V1.1).

## Contexte (lu depuis CLAUDE.md)
§2 (Pennylane v2, polling J+1), §4 (factures gapless `sequences_facturation`, numéro conservé après échec 4xx, avoir autorisé sur facture `payee`, **gating `siret_verification='verifie'`**).

## Périmètre — À coder

### ZD
- [ ] Génération brouillon facture ZD par collecte (mode défaut `par_collecte`)
- [ ] Brouillon mensuel groupé par traiteur si mode `mensuelle` activé (agrégation auto J+1, F2 06.08)
- [ ] Table `factures_collectes` = lignes de facture (`collecte_id` nullable, designation/quantite/taux_tva — F3 06.08)

### AG (unitaire uniquement — pas de mensuel groupé)
- [ ] **Facture achat de pack** (`FPK-YYYY-NNNNN`) générée à la création du pack (mode `globale_achat`)
- [ ] **Facture par collecte** (`FAG-YYYY-NNNNN`) générée à la clôture pour : collecte AG hors pack OU pack `par_collecte`
- [ ] `realisee_sans_collecte` → facture au tarif normal (pas d'attestation, mais facture oui)
- [ ] Champ `reference_affaire` de l'événement transmis à Pennylane comme référence client (facultatif)

### Transverse ZD + AG
- [ ] Numérotation séquentielle **gapless** (`sequences_facturation`) pour les 3 séries FZD / FAG / FPK, numéro conservé après échec 4xx (F4)
- [ ] **Gating facturation** : émission uniquement si `entites_facturation.siret_verification = 'verifie'` ; TVA VIES jamais bloquante (alerte in-app seule)
- [ ] Push Pennylane API v2 après validation Admin (transverse G, wrapper Pennylane)
- [ ] Polling statut J+1 (sans borne temporelle, F2 08) ; gestion avoirs (série `AV-`)
- [ ] Vue `v_factures_client` (masquage marge logistique côté clients, F5 06.08)

## Périmètre — Hors scope (ne PAS toucher)
- [ ] Mode mensuel groupé pour AG → **non prévu** (unitaire uniquement, décision Val 2026-06-14)
- [ ] Webhook Pennylane → V1.1 (V1 = polling)
- [ ] Colonne `marge_logistique` : présente mais masquée par la vue (ne pas exposer)

## Pièges connus / décisions à ne PAS rouvrir
- Gapless = réglementaire : numéro **conservé** après échec 4xx, jamais de trou, jamais de réattribution.
- Avoir autorisé sur facture `payee` (F1 — §05 fait foi).
- Mensuelle = agrégation **auto** J+1 (F2), pas un bouton manuel.
- Trigger anti-double-facturation = prédicat « non facturée » (Reco B 06.08).

## Sources CDC à lire (uniquement — `specs/cdc/` dans le repo)
- `specs/cdc/01 - Cahier des charges App/06 - Fonctionnalités détaillées/08 - Génération et édition facture (Admin).md`
- `specs/cdc/01 - Cahier des charges App/08 - APIs et intégrations.md` (Pennylane v2, polling)
- `specs/cdc/01 - Cahier des charges App/05 - Règles métier.md` (numérotation, avoirs, gating)

## Tests à passer
- Scénarios : `specs/tests/06.08-generation-edition-facture-scenarios.md` (53 scénarios, inclut AG) + `specs/tests/08-apis-integrations-scenarios.md` (Pennylane). **Manifest `specs/manifests/M1.7.json`** (inclut AG — M2.6 supprimé, fusionné ici).

## Mocks consommés
**Mock Pennylane v2 (0.11)** en CI (201/4xx/5xx, statuts polling). Sandbox Pennylane réelle = vérif manuelle au checkpoint, jamais dans le /goal.

## Définition de fini
- [ ] Manifest M1.7 vert ; CI verte (lint + types + Vitest + pgTAP + build)
- [ ] Démo ZD : collecte ZD `cloturee` → brouillon facture FZD (numéro gapless) → validation Admin → push visible dans la **sandbox** Pennylane (manuel) ; mensuel groupé agrégé ; avoir possible
- [ ] Démo AG pack : création pack → brouillon facture FPK → push Pennylane
- [ ] Démo AG collecte : collecte AG hors pack `cloturee` → brouillon FAG → push Pennylane ; `reference_affaire` transmise si renseignée

## Pré-requis d'entrée
- [ ] M1.6 mergé ; transverse G (wrapper Pennylane) ; **mock Pennylane (0.11)** ; sandbox Pennylane en Vault (vérif manuelle)
- [ ] `specs/` synchronisé ; manifest M1.7 présent

## Condition d'achèvement /goal
> /goal Le module 1.7 est terminé quand `pnpm test:module M1.7` est vert (échoue si 0 test ; inclut brouillon ZD collecte + mensuel groupé + brouillon AG par pack FPK + brouillon AG par collecte FAG + gapless 3 séries + conservation numéro après 4xx + avoir + gating SIRET + reference_affaire Pennylane) ET `pnpm check:coverage M1.7` est vert ET les pgTAP (vue masquage marge) sont verts ET `pnpm build` passe ET `pnpm lint` ne renvoie aucune erreur.

## Budget tokens estimé
~1 000k (L+) — AG unitaire ajouté (était M2.6, fusionné ici 2026-06-14).

## Garde-fous /goal
Trust mode ; un seul /goal ; checkpoint humain avant M1.8 ; stop si +50% ; `/cost` → tracker.

## Si tu hésites
Demande à Val. La numérotation gapless est réglementaire : en cas de doute sur la séquence, stop.
