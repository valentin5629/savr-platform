# Brief Claude Code — Module 1.8 — E2E cycle ZD complet (gate binaire de sortie de verticale)

> Créé 2026-06-11. La « démo cycle ZD complet » de fin de V1 était purement manuelle — aucune condition binaire ne validait la verticale de bout en bout. M1.8 la transforme en test Playwright + assertions DB rejouable en CI. Budget imputé sur la ligne « Tests E2E transverses » (pas de double-comptage).

## Objectif
Un test E2E unique (Playwright + assertions DB) qui rejoue le critère d'acceptation business de la verticale V1 : programmation ZD → outbox → MTS-1 (mock) → pesées → bordereau/rapport → brouillon Pennylane (mock).

## Contexte (lu depuis CLAUDE.md)
§4 (règles SI/ALORS du cycle), §8 (V1 = cycle traiteur ZD démontrable).

## Périmètre — À coder
- [ ] Scénario Playwright « cycle ZD nominal » : login `traiteur_commercial` → programmer collecte ZD → vérifier `outbox_events` E1 → avancer le mock MTS-1 (acceptation, tour, pesées) → vérifier `statut_tms` + `pesees_tournees` → déclencher le batch J+1 (fake timer / trigger manuel) → vérifier bordereau + rapport sur R2 (embargo H+24) → validation Admin → vérifier brouillon facture (mock Pennylane) avec numéro gapless
- [ ] Variante 1 : pesée hors seuil → alerte in-app Admin
- [ ] Variante 2 : multi-camions N=2, 1 tour KO → `realisee` partielle + alerte Ops
- [ ] Variante 3 : pesées incomplètes → skip batch, pas de bordereau
- [ ] Intégration CI : job E2E verticale (bloquant à partir de la fin de V1)

## Périmètre — Hors scope (ne PAS toucher)
- [ ] Aucune feature nouvelle : si le test révèle un trou, retour au module concerné (rollback runbook), pas de fix « en douce » dans M1.8
- [ ] Scénarios AG → gate équivalent en fin de V2

## Sources CDC à lire (uniquement — `specs/cdc/` dans le repo)
- `specs/cdc/01 - Cahier des charges App/02 - Personas et cas d'usage.md` (parcours commercial)
- `02 - Verticales.md` (critère d'acceptation V1 — ce document)

## Tests à passer
- Manifest `specs/manifests/M1.8.json` = 4 scénarios ci-dessus.

## Mocks consommés
Mock MTS-1 + mock Pennylane + mock Resend (0.11). Fake timers pour J+1/embargo.

## Définition de fini
- [ ] Les 4 scénarios E2E verts en CI, reproductibles sur `seed_demo` vierge

## Pré-requis d'entrée
- [ ] M1.1 → M1.7 mergés ; 0.11 livré ; `pnpm seed:demo` passe

## Condition d'achèvement /goal
> /goal Le module 1.8 est terminé quand `pnpm test:module M1.8` est vert (échoue si 0 test ; 4 scénarios E2E) ET `pnpm check:coverage M1.8` est vert ET le job CI E2E verticale est câblé et vert ET `pnpm build` passe ET `pnpm lint` ne renvoie aucune erreur.

## Budget tokens estimé
~300k (M) — imputé sur la ligne « Tests E2E transverses » (1,9M).

## Garde-fous /goal
Trust mode ; un seul /goal ; **checkpoint humain = fin de V1 : démo complète à Val + audit éventuel frère** ; stop si +50% ; `/cost` → tracker.

## Si tu hésites
Demande à Val. Un échec E2E = bug d'un module amont : documenter, revenir au module, ne pas contourner dans le test.
