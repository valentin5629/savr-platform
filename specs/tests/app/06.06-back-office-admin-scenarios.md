# Scénarios de test — §06.06 Back-office Admin Savr (+ Packs AG §05 §3)

**Source CDC** : §06.06 (9 sections back-office) + §05 (§3 Packs AG, R_revenus_imputation_organisation, R_compatibilite_vehicule_lieu, R_lieux_admin_only_fields, R_code_mts1_requis, R_lieu_modif_pending, R_controle_acces_cascade, Tarifs et remises — résolution du prix) + §04 (`packs_antgaspi`, `tarifs_packs_ag`, `grilles_tarifaires_zd`, `tarifs_negocie`, `coefficients_perte_labo`, `parametres_taux_recyclage(+history)`, `parametres_facteurs_co2*`, `collectes.dirty_tms`, **`audit_log` créée F1**) + §08 §10 (dispatch manuel, revenus) + §09 (matrice étendue `ops_savr`, §7 Impersonation)
**Généré le** : 2026-06-07
**Statut** : À implémenter par Claude Code

> **Instructions Claude Code** : ces scénarios sont la source de vérité pour les tests du module §06.06.
> Pour chaque scénario :
> - Couche `db` → écrire un test pgTAP dans `supabase/tests/`
> - Couche `api` → écrire un test Vitest dans `packages/plateforme/tests/api/`
> - Couche `ui` → écrire un test Playwright dans `packages/plateforme/tests/e2e/`
> Les tests P1-critique sont bloquants CI. Les tests P2 et P3 sont non-bloquants V1.
> Ne pas démarrer le développement du module sans avoir écrit les tests P1 d'abord.

**Décisions Val 2026-06-07 (6 floues tranchées, propagées §06.06 + §04 + §05 + §08 + §09)** :
- **F1 BLOQUANT** : table **`audit_log` créée dans §04** (référencée ~40× sans définition ; résidu `audit_logs` §05 corrigé). Append-only, INSERT triggers/serveur only, SELECT admin+ops.
- **F2** : Packs AG — **« Ajuster crédits » et « Annuler le pack » ouverts à `ops_savr`** (la matrice §09 fait foi ; §06.06 §8 ex-admin-only aligné).
- **F3** : Transporteurs — **édition SIREN + désactivation `actif=false` ouvertes à `ops_savr`** (matrice §09 fait foi ; 2 tests pgTAP contraires retirés ; ne pas re-proposer la restriction).
- **F4** : carte KPI « Collectes non transmises au TMS » volet AG **gardée telle quelle** — compte toutes les AG `non_envoye`, file d'attribution nominale comprise (assumé : pas un indicateur d'échec côté AG ; recouvre le chip « AG en attente attribution » ; ne pas re-proposer).
- **F5** : terminologie « émission S7 » (sortant App→TMS) **renommée « émission dispatch »** (§06.06 Bloc 0, §08 §10.1, §04 `dirty_tms`) — S7 ne désigne plus que le webhook TMS→App `plaque-saisie`.
- **F6** : bouton **« Fusionner 2 organisations » retiré V1** (script SQL assisté hors UI ; ligne matrice §09 retirée ; question ouverte fermée ; UI = V1.1).

**Résidus corrigés même session** : ligne matrice §09 « Relancer facture » + récap #9 §06.06 purgés (relances Pennylane, décision 2026-04-28) ; compteur templates §06.06 §9 corrigé 18 → 19 actifs.

---

## Résumé de couverture

| Catégorie | Nb scénarios | Couverture |
|-----------|-------------|------------|
| 1. Happy path | 9 | Dashboard Admin/Client, collectes admin, pack AG, association, transporteur, lieu, taux recyclage |
| 2. Cas limites métier | 10 | Bornes pack (épuisement, bandeau <5), coefficient labo, taux 0-1, mix 100 %, remises, 48h |
| 3. Cas d'erreur métier | 10 | Pack actif doublon, motifs, code MTS-1, description asso, versioning, dispatch 409 |
| 4. Isolation données (RLS / permissions) | 12 | ops_savr (admin-only restants), traiteur/gestionnaire deny, colonnes lieux, audit_log, impersonation |
| 5. Idempotence et états | 9 | Recrédit auto, débit tardif, dirty_tms, versioning grilles/tarifs, history immuable, Idempotency-Key |
| 6. Cross-app (Plateforme ↔ TMS) | 4 | Dispatch idempotent, échec E1 → KPI, S2/S3 → chips + dérivation `validee`, 409 statuts terminaux |
| 7. Migration (Bubble → Supabase) | 4 | Packs uniques actifs, grille seed, enums lieux NULL, idempotence re-run |
| **TOTAL** | **58** | |

> **Note périmètre** : la **génération de facture** (workflow Pennylane, avoirs) = lot ⑧ §06.08 ; l'**algo d'attribution AG** (scoring, auto-accept) = lot ⑨ §06.09 ; les **dashboards §11** = lot ⑪. Ici : uniquement la couche back-office (listes, actions, permissions) et les règles packs AG §05 §3 non couvertes par le lot ① (le débit annulation tardive F2-§06.01 et le blocage formulaire sans pack sont déjà testés au lot ①).

---

## Scénarios

### Catégorie 1 — Happy path

```gherkin
# Source : §06.06 §1 Bloc 1 / §04 collectes
# Couche : db
# Priorité : P1-critique

Scénario : dashboard_admin_cartes_kpi_valeurs_correctes
  Étant donné 1 collecte ZD `programmee` avec `statut_tms='non_envoye'` et `tms_reference IS NULL` (échec E1)
  Et 2 collectes AG `programmee` avec `statut_tms='non_envoye'` (file d'attribution nominale)
  Et 1 collecte `statut_tms='attribuee_en_attente_acceptation'`
  Et 1 collecte avec `dirty_tms=true`
  Et 1 collecte ZD et 1 collecte AG `validee` avec `date_collecte = now() + 24h`
  Quand l'Admin ouvre le Dashboard Admin
  Alors la carte « Non transmises au TMS » affiche ZD=1 / AG=2 (F4 : les AG en attente d'attribution comptent)
  Et la carte « En attente validation prestataire » affiche 1
  Et la carte « Modifiées sans renvoi TMS » affiche 1
  Et les cartes « ZD 48h » et « AG 48h » affichent 1 chacune
```

```gherkin
# Source : §06.06 §1 Bloc 2.2 / §05 R_revenus_imputation_organisation
# Couche : api
# Priorité : P1-critique

Scénario : revenus_imputation_organisation_programmatrice
  Étant donné une collecte ZD programmée par l'agence Magnifique Society pour le traiteur shadow Riviera
  Et une facture `emise` de 600 € HT liée à cette collecte (`factures_collectes.montant_ht=600`)
  Et `date_collecte` dans la période interrogée
  Quand l'Admin appelle GET /api/v1/admin/dashboard/revenus-organisations?from=&to=
  Alors la ligne « Magnifique Society » (programmatrice) porte nb_zd=1 et montant_zd=600.00
  Et aucune ligne « Riviera » (traiteur opérationnel shadow) n'apparaît
```

```gherkin
# Source : §06.06 §1 Bloc 2.2
# Couche : api
# Priorité : P2-important

Scénario : revenus_filtre_par_date_collecte_pas_emission
  Étant donné une collecte ZD du 30 avril facturée le 5 mai (facture `emise`, 450 €)
  Quand l'Admin interroge le tableau revenus sur la période 1-30 avril
  Alors la ligne de l'organisation inclut les 450 € (filtre = `date_collecte`, pas `date_emission`)
  Et une interrogation sur la période 1-31 mai ne les inclut pas
```

```gherkin
# Source : §06.06 §2 Dashboard Client
# Couche : ui
# Priorité : P2-important

Scénario : dashboard_client_toutes_organisations_lecture_seule
  Étant donné un `ops_savr` connecté et des collectes réparties sur 3 organisations
  Quand il ouvre le Dashboard Client avec le sélecteur « Toutes les organisations »
  Alors les KPIs agrègent la totalité des collectes Savr (réplique §06.05, sans filtre RLS gestionnaire)
  Et aucune action d'écriture n'est disponible sur cette vue
  Et sa sélection d'organisations est restaurée depuis localStorage à la prochaine ouverture
```

```gherkin
# Source : §06.06 §8 onglet Packs AG / §04 packs_antgaspi / §05 §3
# Couche : api
# Priorité : P1-critique

Scénario : creation_pack_30_standard
  Étant donné l'organisation Kaspia (traiteur) sans pack `actif`
  Et la ligne active `tarifs_packs_ag` type `pack_30` (30 crédits, 460 €, 13 800 €)
  Quand l'Admin crée un Pack 30 en `mode_facturation='globale_achat'` via POST /api/v1/admin/packs-antgaspi avec Idempotency-Key
  Alors un `packs_antgaspi` est créé avec `statut='actif'`, `credits_initiaux=30`, `credits_consommes=0`, `montant_total_ht=13800.00` figé
  Et un brouillon de facture d'achat est généré (`facture_achat_id` renseigné à l'émission — détail lot ⑧)
  Et les crédits sont alloués en totalité immédiatement (pas de calendrier de mensualisation côté plateforme)
```

```gherkin
# Source : §06.06 §5 Associations
# Couche : ui
# Priorité : P2-important

Scénario : creation_association_complete
  Étant donné un `ops_savr` sur le formulaire Association
  Quand il saisit nom, adresse (géocodée), contact, emails de prévenance, horaires 7 lignes, capacité max 500 repas, description ≥ 30 caractères (SIREN facultatif, non requis)
  Alors la fiche est créée `actif=true` avec `horaires_ouverture` stocké en JSON
  Et la description est copiée telle quelle dans les futurs rapports d'impact AG
```

```gherkin
# Source : §06.06 §6 Transporteurs / §05 R_code_mts1_requis
# Couche : api
# Priorité : P1-critique

Scénario : creation_transporteur_mts1_avec_code
  Étant donné un `ops_savr` sur le formulaire Transporteur
  Quand il crée « Strike » avec SIREN 9 chiffres valide, téléphone, `types_vehicules=['fourgon','poids_lourd']`, `type_tms='mts1'` et `code_transporteur_mts1='STR-123'`
  Alors la fiche est créée et le champ code MTS-1 est accepté (obligatoire car `type_tms='mts1'`)
  Et le bouton du Bloc 0 §3 pour ses collectes sera « Envoyer à MTS-1 » (fork V1)
```

```gherkin
# Source : §06.06 §7 Lieux — Normaliser un lieu
# Couche : ui
# Priorité : P2-important

Scénario : normalisation_lieu_saisi_manuellement
  Étant donné un lieu créé `actif=false` par un traiteur pendant la programmation
  Quand l'Admin complète la fiche (type_vehicule_max, enums accès) et enregistre avec `actif=true`
  Alors le lieu devient sélectionnable au formulaire §06.01
  Et l'action est tracée dans `audit_log`
```

```gherkin
# Source : §06.06 §9 Taux de recyclage par filière / §08 §9
# Couche : api
# Priorité : P1-critique

Scénario : modification_taux_captation_filiere
  Étant donné la filière Verre à `taux_captation=0.96`
  Quand l'`admin_savr` appelle PUT /api/v1/admin/parametres/taux-recyclage/{filiere_id} avec `taux_captation=0.92` et `commentaire_modif='Citeo 2026'` (≥ 5 car.) et Idempotency-Key
  Alors `parametres_taux_recyclage` est mis à jour (taux, `date_maj`, `updated_at`)
  Et une ligne `parametres_taux_recyclage_history` est insérée par trigger (avant/après + `modifie_par` + commentaire)
  Et aucune collecte déjà clôturée n'est recalculée (`caps_appliques` et PDF inchangés)
```

### Catégorie 2 — Cas limites métier

```gherkin
# Source : §05 §3 Débit d'un crédit / §04 packs_antgaspi
# Couche : db
# Priorité : P1-critique

Scénario : pack_dernier_credit_bascule_epuise
  Étant donné un pack `actif` de Kaspia avec `credits_initiaux=10` et `credits_consommes=9`
  Quand la 10e collecte AG passe à `realisee` (débit +1)
  Alors `credits_consommes=10` et `statut` bascule automatiquement à `epuise`
  Et `credits_restants` (colonne GENERATED) vaut 0
  Et une notification Admin « pack épuisé, renouvellement requis » est émise
```

```gherkin
# Source : §06.06 §8 Bandeau « À relancer — Packs < 5 crédits »
# Couche : ui
# Priorité : P3-nominal

Scénario : bandeau_pack_bas_borne_5_credits
  Étant donné un pack `actif` avec `credits_restants=5`
  Quand l'Admin ouvre l'onglet Packs AG de la fiche organisation
  Alors aucun bandeau n'est affiché (seuil strict < 5)
  Quand `credits_restants` passe à 4
  Alors le bandeau « Pack [type] — 4 crédits restants. Dernier achat : [date] » s'affiche
  Et aucun email automatique n'est envoyé (pas de template dédié V1)
```

```gherkin
# Source : §06.06 §8 Ajuster crédits / §05 §3 garde-fou
# Couche : db
# Priorité : P1-critique

Scénario : ajustement_credits_initiaux_sous_consommes_refuse
  Étant donné un pack avec `credits_initiaux=30` et `credits_consommes=12`
  Quand un staff tente « Ajuster crédits » avec nouveau `credits_initiaux=10`
  Alors l'opération est refusée (422 — violerait l'invariant `credits_consommes ≤ credits_initiaux`, CHECK DB en double sécurité)
  Et un ajustement à `credits_initiaux=12` (= consommés) est accepté et bascule le pack en `epuise`
```

```gherkin
# Source : §06.06 §9 Tarifs AG — modal Modifier
# Couche : api
# Priorité : P2-important

Scénario : tarif_pack_valide_du_aujourdhui_accepte
  Étant donné la grille `pack_10` active depuis le 01/01/2026
  Quand l'`admin_savr` modifie avec `valide_du = date du jour` (borne autorisée : ≥ aujourd'hui)
  Alors la ligne actuelle est fermée à `valide_du - 1 jour` et la nouvelle ligne créée
  Et une tentative avec `valide_du = hier` est rejetée en 422
```

```gherkin
# Source : §04 tarifs_negocie / §05 Tarifs et remises
# Couche : db
# Priorité : P1-critique

Scénario : remise_pct_bornes_check
  Étant donné le formulaire Remises négociées
  Quand l'Admin crée une remise `remise_pct=1.00` (100 %)
  Alors la ligne est acceptée (CHECK `> 0 AND <= 1`)
  Quand il tente `remise_pct=0`
  Alors l'INSERT échoue (borne 0 exclue)
  Quand il tente `remise_pct=1.01`
  Alors l'INSERT échoue
```

```gherkin
# Source : §05 Tarifs et remises — résolution du prix (exemple canonique)
# Couche : api
# Priorité : P1-critique

Scénario : cumul_remises_multiplicatif_viparis
  Étant donné Butard sur grille « Forfait + variable » (200 € + 1 €/pax) avec 300 pax chez Viparis
  Et une remise `scope=gestionnaire` Viparis −5 % (`lieu_id=null`)
  Et une remise `scope=organisation` Butard −10 % active
  Quand le prix de la collecte est résolu à la facturation
  Alors base = 200 + 300 = 500 € et prix final = 500 × 0,95 × 0,90 = 427,50 € HT
  Et `factures_collectes.tarif_detail` fige base + les 2 remises appliquées
```

```gherkin
# Source : §06.06 §8 Coefficient perte labo
# Couche : db
# Priorité : P2-important

Scénario : coefficient_labo_unique_par_annee
  Étant donné un coefficient 2025 = 0,1500 saisi pour Kaspia
  Quand l'`admin_savr` tente d'ajouter un second coefficient pour l'année de référence 2025
  Alors l'INSERT échoue (contrainte `UNIQUE (organisation_id, annee_reference)`) avec message explicite
  Et la colonne « Appliqué aux événements de » affiche 2026 (= année + 1, calculée)
```

```gherkin
# Source : §06.06 §9 Taux de recyclage — validation serveur
# Couche : api
# Priorité : P2-important

Scénario : taux_captation_bornes_0_1
  Quand l'`admin_savr` soumet `taux_captation=1.0`
  Alors la modification est acceptée (borne incluse)
  Quand il soumet `taux_captation=1.01`
  Alors 422 « Le taux doit être compris entre 0 et 1 »
  Quand il soumet `commentaire_modif` de 4 caractères
  Alors 422 (minimum 5 caractères)
```

```gherkin
# Source : §06.06 §9 Facteurs CO₂ — Mix emballages
# Couche : ui
# Priorité : P2-important

Scénario : mix_emballages_somme_100_bloquante
  Étant donné l'écran Mix emballages (7 matériaux)
  Quand l'Admin modifie les `part_pct` pour une somme de 99,9 %
  Alors le bouton Enregistrer est bloqué avec contrôle live « somme ≠ 100 % »
  Quand la somme vaut exactement 100 %
  Alors l'enregistrement passe, l'agrégat emballage est recalculé et la ligne `emballage` de `parametres_facteurs_co2` reste non éditable directement (dérivée)
```

```gherkin
# Source : §06.06 §1 Bloc 1 — cartes 48h
# Couche : db
# Priorité : P3-nominal

Scénario : carte_48h_borne_exacte
  Étant donné une collecte ZD `validee` avec `date_collecte` à exactement now() + 48h
  Quand le KPI « ZD 48h » est calculé (`BETWEEN now() AND now() + interval '48 hours'`)
  Alors la collecte est comptée (borne incluse)
  Et une collecte à now() + 48h + 1min n'est pas comptée
  Et une collecte `annulee` dans la fenêtre n'est pas comptée (statut ∉ programmee/validee)
```

### Catégorie 3 — Cas d'erreur métier

```gherkin
# Source : §05 §3 Pack unique actif / §04 uniq_pack_actif_par_org
# Couche : db
# Priorité : P1-critique

Scénario : creation_pack_refusee_si_pack_actif_existant
  Étant donné Kaspia avec un pack `actif` (12 crédits restants)
  Quand l'Admin tente de créer un nouveau pack via le modal
  Alors la validation inline bloque : « Kaspia a déjà un pack actif (Pack 30 — 12 crédits restants). Annulez-le avant d'en créer un nouveau. »
  Et un INSERT SQL direct concurrent échoue sur l'index partiel `uniq_pack_actif_par_org` (erreur 23505)
```

```gherkin
# Source : §06.06 §8 Annuler le pack
# Couche : api
# Priorité : P1-critique

Scénario : annulation_pack_avec_credits_consommes_warning
  Étant donné un pack `actif` avec `credits_consommes=8`
  Quand un staff annule le pack avec motif ≥ 10 caractères
  Alors `statut='annule'` + audit_log
  Et le warning UI « 8 collectes ont déjà été imputées sur ce pack. L'annulation ne régénère pas les crédits côté collectes » est affiché
  Et les 8 collectes conservent leur `pack_antgaspi_id` (traçabilité)
  Quand le motif fait 9 caractères
  Alors l'action est refusée en 422 (≥ 10 car.)
```

```gherkin
# Source : §06.06 §8 — credits_consommes non éditable
# Couche : api
# Priorité : P1-critique

Scénario : edition_directe_credits_consommes_refusee
  Étant donné un pack `actif`
  Quand un client API (même `admin_savr`) tente un UPDATE direct de `packs_antgaspi.credits_consommes`
  Alors l'écriture est refusée (incrément exclusivement via débit collecte / triggers)
  Et le seul chemin d'ajustement est « Ajuster crédits » qui modifie `credits_initiaux`
```

```gherkin
# Source : §08 §10.1 — erreurs dispatch
# Couche : api
# Priorité : P1-critique

Scénario : dispatch_statuts_terminaux_409
  Étant donné une collecte `realisee`
  Quand un staff appelle POST /api/v1/admin/collectes/{id}/dispatch
  Alors la réponse est 409 (idem pour `cloturee` et `annulee`)
  Et un appel sur un `collecte_id` inconnu retourne 404
```

```gherkin
# Source : §08 §10.1 — override sans motif
# Couche : api
# Priorité : P1-critique

Scénario : override_prestataire_sans_motif_422
  Étant donné une collecte AG dont le top 1 algo est A Toutes!
  Quand l'`admin_savr` envoie `prestataire_id=Marathon` sans `motif_override`
  Alors la réponse est 422 (motif obligatoire si choix ≠ top 1, ≥ 5 caractères)
  Et avec `motif_override='Vélo indisponible ce jour'` l'envoi passe et `collectes.motif_override_prestataire` est renseigné + audit_log
```

```gherkin
# Source : §05 R_code_mts1_requis
# Couche : api
# Priorité : P1-critique

Scénario : transporteur_mts1_sans_code_bloque
  Quand un staff tente d'enregistrer un transporteur `type_tms='mts1'` sans `code_transporteur_mts1`
  Alors la saisie est bloquée (contrôle formulaire)
  Et si un envoi MTS-1 est tenté vers un transporteur mts1 sans code (donnée legacy), l'envoi est refusé avec erreur explicite et la collecte reste `statut_tms='non_envoye'`
```

```gherkin
# Source : §06.06 §5 — description rapport impact
# Couche : api
# Priorité : P2-important

Scénario : association_description_trop_courte
  Quand un staff crée une association avec `description_rapport_impact` de 29 caractères
  Alors la création est refusée (validation ≥ 30 caractères, colonne NOT NULL)
```

```gherkin
# Source : §04 tarifs_negocie — règles
# Couche : db
# Priorité : P1-critique

Scénario : suppression_remise_utilisee_impossible
  Étant donné une remise `tarifs_negocie` déjà figée dans une `factures_collectes.tarif_detail`
  Quand l'Admin tente de la supprimer
  Alors la suppression est refusée — seul le versioning est permis (fermeture `valide_jusqu_au` + nouvelle ligne)
  Et une remise jamais utilisée peut être fermée normalement
```

```gherkin
# Source : §04 grilles_tarifaires_zd — est_defaut
# Couche : db
# Priorité : P1-critique

Scénario : grille_defaut_unicite
  Étant donné la grille « Standard paliers » `est_defaut=true` active
  Quand un INSERT crée une seconde grille active avec `est_defaut=true`
  Alors l'INSERT échoue sur l'index partiel `WHERE est_defaut=true AND valide_jusqu_au IS NULL`
```

```gherkin
# Source : §06.06 §3 Bloc 6 — Annuler le crédit
# Couche : ui
# Priorité : P2-important

Scénario : annuler_credit_conditionne_statut_realisee
  Étant donné une collecte AG `validee` (pas encore réalisée)
  Quand un staff ouvre le Bloc 6 Facturation détaillée
  Alors le bouton « Annuler le crédit de cette collecte » est absent (condition : `statut='realisee'` uniquement)
  Et sur une collecte `realisee`, l'action exige un motif ≥ 10 caractères
```

### Catégorie 4 — Isolation données (RLS / permissions)

```gherkin
# Source : §09 Matrice ops_savr — Collectes
# Couche : api
# Priorité : P1-critique

Scénario : ops_override_prestataire_403
  Étant donné un `ops_savr` connecté
  Quand il appelle POST /api/v1/admin/collectes/{id}/dispatch avec `prestataire_id` fourni
  Alors la réponse est 403 (override admin-only)
  Et le même appel SANS `prestataire_id` (renvoi simple) réussit (reset `dirty_tms`)
```

```gherkin
# Source : §09 Matrice ops_savr — Factures
# Couche : api
# Priorité : P1-critique

Scénario : ops_factures_actions_sensibles_refusees
  Étant donné un `ops_savr` sur une facture `brouillon`
  Alors il peut valider + envoyer Pennylane et télécharger le PDF
  Quand il tente d'éditer une ligne/montant, d'annuler la facture ou de générer un avoir
  Alors chaque action est refusée (403 / bouton masqué) — admin-only
```

```gherkin
# Source : §09 Matrice ops_savr — Packs AG (F2 tranché Val 2026-06-07)
# Couche : api
# Priorité : P1-critique

Scénario : ops_packs_ag_acces_complet
  Étant donné un `ops_savr` sur l'onglet Packs AG de Kaspia
  Quand il crée un pack, ajuste les crédits (motif ≥ 10 car.) puis annule le pack (motif ≥ 10 car.)
  Alors les 3 actions réussissent (F2 : matrice §09 fait foi, ops = admin sur les packs)
  Et chaque action est tracée dans `audit_log` avec `role_auteur='ops_savr'`
```

```gherkin
# Source : §09 Matrice ops_savr — Associations
# Couche : db
# Priorité : P1-critique

Scénario : ops_association_champs_proteges
  Étant donné un `ops_savr` sur une fiche association
  Alors il peut modifier contacts, horaires, instructions, capacité, description
  Quand il tente de modifier le SIREN, l'habilitation 2041-GE ou `actif=false`
  Alors chaque écriture est refusée (admin-only — inchangé par F3 qui ne porte que sur transporteurs/lieux)
```

```gherkin
# Source : §09 Matrice ops_savr — Transporteurs/Lieux (F3 tranché Val 2026-06-07)
# Couche : db
# Priorité : P1-critique

Scénario : ops_transporteurs_lieux_acces_complet
  Étant donné un `ops_savr`
  Quand il modifie le SIREN d'un transporteur puis le désactive (`actif=false`)
  Alors les 2 écritures réussissent (F3 : restrictions levées, ex-tests pgTAP retirés)
  Et il peut créer/modifier/désactiver un lieu, y compris les 4 champs admin/ops only
```

```gherkin
# Source : §09 Matrice ops_savr — Organisations / Users
# Couche : api
# Priorité : P1-critique

Scénario : ops_actions_sensibles_organisations_users
  Étant donné un `ops_savr`
  Quand il tente : édition `tarif_refacture_pax_zd`, hard delete user, promotion d'un user en `admin_savr`, impersonation
  Alors les 4 actions sont refusées (admin-only)
  Et créer/inviter/suspendre un user et changer un rôle non-admin réussissent
```

```gherkin
# Source : §09 Matrice ops_savr — Paramètres §9
# Couche : db
# Priorité : P1-critique

Scénario : ops_parametres_lecture_seule
  Étant donné un `ops_savr` sur §9 Paramètres
  Alors toutes les sous-sections sont lisibles avec bandeau « Lecture seule — édition réservée admin »
  Quand il tente d'écrire : tarifs ZD/AG, grilles catalogue, remises, taux recyclage, facteurs CO₂, mix emballages, coefficient labo, templates emails, référentiels, intégrations
  Alors chaque écriture est refusée (403 / RLS deny — admin_savr only)
```

```gherkin
# Source : §05 R_lieux_admin_only_fields / §09 pgTAP lieux_admin_only_fields_hidden_from_clients
# Couche : db
# Priorité : P1-critique

Scénario : champs_lieux_internes_invisibles_clients
  Étant donné un user `traiteur_manager` Kaspia
  Quand il SELECT `plateforme.lieux.commentaire_lieu` (ou `siren`, `email_gestionnaire`, `reference_citeo`)
  Alors la requête échoue (privilege error column-level)
  Et `v_lieux_public` ne contient aucune de ces 4 colonnes
  Et ces colonnes ne figurent ni dans le payload dispatch TMS ni dans les exports CSV clients
```

```gherkin
# Source : §09 §3ter / §06.06 — accès back-office
# Couche : api
# Priorité : P1-critique

Scénario : profils_clients_deny_backoffice
  Étant donné un `traiteur_manager`, un `gestionnaire_lieux` et un `agence_manager`
  Quand chacun tente d'accéder aux routes /api/v1/admin/* (dashboard revenus, dispatch, packs, paramètres)
  Alors chaque appel est refusé en 403 (middleware requireRole admin/ops)
  Et un `traiteur_manager` Kaspia ne voit jamais les collectes Kardamome via les listes back-office (RLS organisation)
```

```gherkin
# Source : §09 coefficients_perte_labo — lecture indirecte
# Couche : db
# Priorité : P1-critique

Scénarios : coefficient_labo_jamais_expose_brut
  Étant donné un `gestionnaire_lieux` Viparis
  Quand il SELECT `coefficients_perte_labo`
  Alors la requête est refusée (deny direct, table admin/ops only en lecture)
  Et la fonction SECURITY DEFINER `f_dechets_labo_estimes(evenement_id)` ne lui retourne que la valeur kg (jamais le coefficient brut), après contrôle d'appartenance du lieu
```

```gherkin
# Source : §04 audit_log (F1) / §09 matrice Audit log
# Couche : db
# Priorité : P1-critique

Scénario : audit_log_append_only
  Étant donné la table `audit_log` (créée F1)
  Quand un `admin_savr` tente INSERT, UPDATE ou DELETE direct via l'API
  Alors les 3 opérations sont refusées (écriture trigger DB / serveur uniquement)
  Et `admin_savr` + `ops_savr` peuvent SELECT ; tout autre rôle est deny
```

```gherkin
# Source : §09 §7 Impersonation / §04 audit_log
# Couche : api
# Priorité : P1-critique

Scénario : impersonation_tracee_et_bornee
  Étant donné un `admin_savr` qui impersonne le user manager Kaspia
  Alors un bandeau orange permanent signale la session dans l'UI
  Et chaque action est loggée dans `audit_log` avec `user_id`=manager Kaspia et `impersonator_id`=admin (distincts)
  Et la session retombe automatiquement sur le compte admin après 1h ou via « Quitter l'impersonation »
  Et un `ops_savr` qui tente d'impersonner reçoit 403
```

### Catégorie 5 — Idempotence et états

```gherkin
# Source : §05 §3 Recrédit automatique / §04 packs_antgaspi
# Couche : db
# Priorité : P1-critique

Scénario : recredit_auto_annulation_post_realisee
  Étant donné un pack `epuise` (30/30) dont la collecte C a consommé le 30e crédit (`pack_antgaspi_id` renseigné)
  Quand l'Admin force C de `realisee` → `annulee` avec `motif_annulation` obligatoire
  Alors le trigger décrémente `credits_consommes` à 29, rebascule le pack `epuise` → `actif`
  Et `collectes.pack_antgaspi_id` passe à NULL
  Et `audit_log` reçoit `action='pack_recredite_annulation_collecte'`
  Et la programmation AG redevient possible pour l'organisation
```

```gherkin
# Source : §05 §3 Articulation débit tardif / recrédit
# Couche : db
# Priorité : P1-critique

Scénario : annulation_tardive_pas_de_double_mouvement
  Étant donné une collecte AG débitée par annulation tardive (trigger `trg_pack_debit_annulation_tardive`, transition → `annulee` depuis non-realisee)
  Alors la collecte porte `pack_antgaspi_id` renseigné et `statut='annulee'`
  Et le trigger de recrédit (`realisee → annulee`) ne s'est PAS exécuté (transition différente) — aucun recrédit
  Et le solde du pack reflète exactement 1 débit
```

```gherkin
# Source : §06.06 §3 Bloc 6 — Annuler le crédit (annulee_cote_savr)
# Couche : db
# Priorité : P1-critique

Scénario : annuler_credit_cote_savr_distinct_annulation
  Étant donné une collecte AG `realisee` débitée sur le pack de Kaspia
  Quand l'Admin clique « Annuler le crédit de cette collecte » avec motif ≥ 10 caractères
  Alors `collectes.annulee_cote_savr=true`, le pack est recrédité, `audit_log` tracé
  Et la collecte RESTE `statut='realisee'` (hors-périmètre commercial, pas d'annulation métier)
  Et elle n'est pas facturée
```

```gherkin
# Source : §06.06 §3 Bloc 0 / §04 dirty_tms (F5 : émission dispatch)
# Couche : db
# Priorité : P1-critique

Scénario : dirty_tms_cycle_complet
  Étant donné une collecte ZD envoyée au TMS (`tms_reference IS NOT NULL`, `dirty_tms=false`)
  Quand l'Admin modifie le pax (champ propagé)
  Alors le trigger `set_collectes_dirty_tms` passe `dirty_tms=true`
  Quand il clique « Renvoyer au TMS » (réémission dispatch §08 §10.1)
  Alors `dirty_tms=false`
  Et une modification d'un champ NON propagé (`notes_internes`) ne déclenche pas le flag
```

```gherkin
# Source : §06.06 §8 modal création pack — Idempotency-Key
# Couche : api
# Priorité : P1-critique

Scénario : creation_pack_idempotency_key
  Quand POST /api/v1/admin/packs-antgaspi est envoyé 2 fois avec la même Idempotency-Key (retry réseau)
  Alors un seul `packs_antgaspi` est créé, la 2e réponse renvoie le même résultat
```

```gherkin
# Source : §06.06 §9 Tarifs AG / §04 tarifs_packs_ag — versioning
# Couche : db
# Priorité : P1-critique

Scénario : versioning_tarifs_packs_non_retroactif
  Étant donné un pack 10 créé à 5 000 € (figé) et la grille `pack_10` active
  Quand l'admin modifie la grille `pack_10` à 520 €/crédit (fermeture + nouvelle ligne)
  Alors l'ancienne ligne porte `valide_jusqu_au = valide_du - 1 jour` (aucun UPDATE de ses tarifs)
  Et le pack existant conserve `montant_total_ht=5000.00` (snapshot, pas de FK tarif)
  Et un nouveau pack 10 pré-remplit 5 200 €
```

```gherkin
# Source : §06.06 §9 Grilles tarifaires ZD — versioning
# Couche : db
# Priorité : P2-important

Scénario : versioning_grille_zd_fermeture_complete
  Étant donné la grille « Standard paliers » affectée à 3 organisations
  Quand l'admin la modifie
  Alors la grille est fermée (`valide_jusqu_au`) et une nouvelle grille (entête + lignes) est créée
  Et les factures passées conservent leur prix via `factures_collectes.tarif_detail` (jamais recalculées)
```

```gherkin
# Source : §04 parametres_taux_recyclage_history (+ pattern facteurs CO₂)
# Couche : db
# Priorité : P1-critique

Scénario : history_immuable
  Étant donné une ligne `parametres_taux_recyclage_history`
  Quand un `admin_savr` tente UPDATE ou DELETE
  Alors l'opération est refusée (immuable — audit réglementaire)
  Et un INSERT direct via API est refusé (insertion par trigger uniquement)
  Et le même invariant vaut pour `parametres_facteurs_co2_history`, `parametres_mix_emballages_history`, `parametres_facteurs_co2_ag_history`
```

```gherkin
# Source : §06.06 §7 Worklist modifs lieu / §05 R_lieu_modif_pending
# Couche : ui
# Priorité : P2-important

Scénario : worklist_lieu_auto_resolutive
  Étant donné une collecte avec `lieu_overrides` divergeant du lieu officiel (adresse corrigée)
  Alors la ligne apparaît dans la worklist avec diff avant/après et le badge nav est incrémenté
  Quand l'Admin clique « Appliquer au lieu » et enregistre la fiche pré-remplie
  Alors le `plateforme.lieux` officiel est mis à jour et la ligne disparaît (auto-résolutive, pas de statut stocké)
  Et le snapshot TMS de la collecte courante n'est PAS re-propagé (figé) — la correction vaut pour les futures collectes
```

### Catégorie 6 — Scénarios cross-app (Plateforme ↔ TMS)

> Périmètre : §06.06 ne porte pas d'endpoint E/S propre — il consomme le contrat existant (E1/E2 sortants via dispatch §08 §10.1 ; webhooks S2/S3 entrants pour les chips). L'enveloppe complète (HMAC, X-API-Version, retry) est testée aux lots TMS M01-M05 et au lot ① §06.01. Couverture ciblée ici.

```gherkin
# Source : §08 §10.1 / §06.06 §3 Bloc 0
# Couche : api
# Priorité : P1-critique

Scénario : dispatch_reemission_idempotente
  Étant donné une collecte ZD déjà envoyée (`tms_reference='TMS-001'`, `dirty_tms=true`)
  Quand l'Admin clique 2 fois « Renvoyer au TMS » (double-clic)
  Alors un seul effet métier côté TMS (réémission idempotente, même `tms_reference`)
  Et `dirty_tms=false` après le premier appel, inchangé au second
```

```gherkin
# Source : §06.06 §1 Bloc 1 carte 1 / §04 statut_tms
# Couche : api
# Priorité : P1-critique

Scénario : echec_e1_alimente_carte_kpi
  Étant donné une soumission ZD dont l'appel E1 POST /collectes échoue (500 TMS, retries épuisés)
  Alors `statut_tms` reste `non_envoye` et `tms_reference IS NULL`
  Et la collecte apparaît dans la carte « Collectes non transmises au TMS » volet ZD et le chip correspondant
  Quand un « Renvoyer au TMS » réussit
  Alors `statut_tms='a_attribuer'`, `tms_reference` renseigné, la carte décrémente
```

```gherkin
# Source : §04 statut_tms / fn_sync_statut_collecte_from_tms / §06.06 chips
# Couche : db
# Priorité : P1-critique

Scénario : acceptation_prestataire_derivee_et_chips
  Étant donné une collecte `programmee` avec `statut_tms='attribuee_en_attente_acceptation'` (comptée chip « En attente prestataire »)
  Quand le webhook d'acceptation TMS fait passer `statut_tms='acceptee'`
  Alors le trigger dérive `statut='validee'` (jamais posé par une validation Admin)
  Et la collecte sort du chip « En attente prestataire »
```

```gherkin
# Source : §08 §10.1 — contrat erreurs
# Couche : api
# Priorité : P2-important

Scénario : dispatch_contrat_erreurs_complet
  Quand POST /api/v1/admin/collectes/{id}/dispatch est appelé sur une collecte `cloturee`
  Alors 409 ; sur id inconnu → 404 ; override ops → 403 ; override sans motif → 422
  Et aucun de ces refus n'émet de flux vers le TMS ni ne modifie `dirty_tms`
```

```gherkin
# Source : §08 §3bis.7 agrégation terminale tous KO / §06.06 file Admin (A2, décision Val 2026-06-15)
# Couche : api + db
# Priorité : P1-critique

Scénario : collecte_tous_camions_ko_intervention_admin_manuelle
  Étant donné une collecte multi-camions dont tous les tours TMS sont terminés CANCELED/KO
  Quand le polling agrège l'état terminal
  Alors collectes.statut_tms = 'rejetee_par_prestataire' et collectes.statut reste 'en_cours'
  Et la collecte est signalée par une alerte Ops in-app (aucune reprogrammation automatique)
  Et l'Admin la traite manuellement : re-dispatch vers un autre prestataire ou reprogrammation
```

```gherkin
# Source : CLAUDE.md §4 Multi-camions N modifiable à chaud par Ops (A3, décisions Val 2026-06-10/11)
# Couche : api + db
# Priorité : P1-critique

Scénario : multi_camions_augmenter_n_cree_rangs_manquants
  Étant donné une collecte (statut=validee, nb_camions_demande=2) avec 2 tournées rangs 1 et 2
  Quand Ops porte nb_camions_demande à 3 (plus d'1h avant la mission)
  Alors seul le rang 3 manquant est créé (1 customerOrder + 1 tournée, clé idempotence reference-3, external_ref_commande stocké par tournée)
  Et les rangs 1 et 2 existants sont inchangés

Scénario : multi_camions_reduire_n_supprime_rangs
  Étant donné une collecte (statut=validee, nb_camions_demande=3) avec 3 tournées
  Quand Ops ramène nb_camions_demande à 2 (plus d'1h avant la mission)
  Alors le rang 3 retiré est supprimé (DELETE de la tournée correspondante)

Scénario : multi_camions_modif_n_bloquee_moins_1h
  Étant donné une collecte multi-camions dont une mission démarre dans moins d'1h
  Quand Ops tente de réduire nb_camions_demande
  Alors la modification est bloquée et une alerte Ops est affichée

Scénario : multi_camions_modif_n_interdite_des_realisee
  Étant donné une collecte statut=realisee
  Quand Ops tente de modifier nb_camions_demande
  Alors la RPC refuse (FOR UPDATE sur collectes, statut IN programmee/validee/en_cours requis ; jamais de régression d'un statut terminal — ajout après coup = flux incident Admin)
```

### Catégorie 7 — Scénarios de migration (Bubble → Supabase)

> Référence : §13 Migration + `04 - Migration/05 - Checks reconciliation.md` (à produire — skill `cdc-migration-data` pas encore lancée, jauge 0 %). Scénarios exécutés sur env dev, dataset `seed_demo`.

```gherkin
# Source : §05 §3 Pack unique actif / §13
# Couche : db
# Priorité : P1-critique

Scénario : migration_packs_unicite_actif
  Étant donné le dataset `seed_demo` simulant les packs Bubble (multi-packs FIFO historiques)
  Quand le script de migration importe les packs
  Alors chaque organisation a au plus 1 pack `statut='actif'` (index `uniq_pack_actif_par_org` respecté)
  Et les packs historiques sont importés `epuise` ou `annule` avec `credits_consommes` cohérents (somme des collectes débitées)
  Et toute violation détectée est listée au rapport de réconciliation (pas d'import silencieusement corrigé)
```

```gherkin
# Source : §04 grilles_tarifaires_zd — seed / §13
# Couche : db
# Priorité : P1-critique

Scénario : migration_grille_standard_seed
  Quand le seed crée la grille « Standard paliers » `est_defaut=true` (5 tranches : 450/600/800/1000/1€-pax)
  Alors les organisations sans grille affectée (`grille_tarifaire_zd_id IS NULL`) résolvent leur prix sur cette grille
  Et les anciennes lignes `tarifs_negocie` à prix absolu sont reprises en grilles dédiées + remises % (selon §04 Migration)
```

```gherkin
# Source : §06.06 §7 / §04 lieux — enums refondus
# Couche : db
# Priorité : P2-important

Scénario : migration_lieux_enums_non_repris
  Quand les lieux Bubble sont migrés
  Alors `stationnement` et `acces_office` sont NULL (nouveaux référentiels, ressaisie manuelle Admin)
  Et `type_vehicule_max` (NOT NULL) est renseigné par le script avec une valeur par défaut documentée + liste de ressaisie au rapport
  Et aucune valeur texte libre Bubble n'est mappée automatiquement vers les enums
```

```gherkin
# Source : §13 / pattern idempotence migration
# Couche : db
# Priorité : P1-critique

Scénario : migration_rerun_idempotent
  Étant donné une migration `seed_demo` réussie (packs, grilles, lieux, associations, transporteurs)
  Quand le script de migration est relancé intégralement
  Alors aucun doublon n'est créé (upsert par identifiant source Bubble)
  Et les compteurs du rapport de réconciliation sont identiques au premier run
```

---

```gherkin
# Source : §06.06 §4bis — saisie manuelle flux ZD Admin (patch M1.8/M1.1b 2026-06-24)
# Couche : api + db
# Priorité : P1-critique

Scénario : zd_collecte_creee_sans_collecte_flux_predefinis
  Étant donné un Admin qui crée une collecte ZD via POST /admin/collectes
  Quand la collecte est créée (statut=programmee)
  Alors aucune ligne collecte_flux n'est pré-créée (COUNT collecte_flux WHERE collecte_id = 0)
  Et l'UI affiche les 5 flux du référentiel (biodechet, emballage, carton, verre, dechet_residuel) en statut « En attente »
```

```gherkin
# Source : §06.06 §4bis — saisie manuelle flux ZD Admin (patch M1.8/M1.1b 2026-06-24)
# Couche : api + db
# Priorité : P1-critique

Scénario : admin_saisie_manuelle_flux_zd_nominal
  Étant donné une collecte ZD realisee sans lignes collecte_flux (pesées TMS non disponibles)
  Quand l'Admin exécute PATCH /admin/collectes/{id}/flux avec [{flux_id: "verre", poids_reel_kg: 120.5, motif: "Saisie manuelle TMS indisponible"}]
  Alors la ligne collecte_flux (verre, 120.5 kg) est créée ou mise à jour (UPSERT par collecte_id + flux_id)
  Et une ligne audit_log est créée (action=flux_saisie_manuelle, user=admin, motif renseigné)
  Et collectes.taux_recyclage est recalculé immédiatement
```

```gherkin
# Source : §06.06 §4bis — saisie manuelle flux ZD Admin (patch M1.8/M1.1b 2026-06-24)
# Couche : db
# Priorité : P2-important

Scénario : admin_saisie_manuelle_flux_zd_bloque_si_cloturee
  Étant donné une collecte ZD cloturee
  Quand l'Admin tente PATCH /admin/collectes/{id}/flux
  Alors la réponse est 409 Conflict (modification impossible — collecte clôturée)
  Et audit_log ne reçoit aucune ligne pour cet appel
```

---

## Scénarios hors scope (à générer en V1.1 ou couverts ailleurs)

- **Génération/édition facture, flux Pennylane, avoirs** → lot ⑧ (§06.08) — ici seules les permissions ops/admin sur la liste sont testées.
- **Algo attribution AG** (filtres éligibilité, tri distance, auto-accept, re-jeu) → lot ⑨ (§06.09). `R_compatibilite_vehicule_lieu` y sera testé côté algo (ici : seulement la donnée transporteur).
- **Dashboards §11 / Reporting §12** (KPIs métier, benchmark, exports) → lots ⑪/⑫.
- **Blocage programmation AG sans pack + débit annulation tardive côté formulaire** → déjà couverts lot ① (§06.01, F2/F3 2026-06-07).
- **Fusion d'organisations** → retirée V1 (F6) — scénarios à écrire en V1.1 avec la spec UI.
- **Relance facture en retard** → inexistante V1 (Pennylane).
- **Simulateur d'attribution** → V2.
- **File normalisation types événements** → mécanisme supprimé (Sujet 4).
- **Cat.7** : les scénarios rollback/corruption fine seront étoffés quand `cdc-migration-data` aura produit `05 - Checks reconciliation.md` (phase 10 du pipeline, 0 % à ce jour) — les 4 scénarios ci-dessus fixent les invariants minimaux.

## Recommandations à confirmer (non bloquantes)

1. **Fenêtre « collectes récentes » de la worklist modifs lieu** (§06.06 §7) : « récentes » non chiffré. Reco : fenêtre glissante 60 jours (au-delà, l'écart est considéré obsolète). À confirmer avant dev — sinon Claude Code choisira arbitrairement.
2. **Carte « Packs < 5 crédits » agrégée** : confirmé hors V1 (lecture séquentielle des fiches) — rouvrir V1.1 si volume > 10 orgas concernées.
